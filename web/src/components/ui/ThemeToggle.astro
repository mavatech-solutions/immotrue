---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = '', style } = Astro.props;
---

<button type="button" aria-label="Theme wechseln" class={`theme-toggle ${className}`} style={style} data-theme-toggle>
  <svg class="icon-sun" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="12" cy="12" r="4"></circle>
    <line x1="12" y1="2" x2="12" y2="4"></line>
    <line x1="12" y1="20" x2="12" y2="22"></line>
    <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
    <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
    <line x1="2" y1="12" x2="4" y2="12"></line>
    <line x1="20" y1="12" x2="22" y2="12"></line>
    <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
    <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
  </svg>
  <svg class="icon-moon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
  </svg>
</button>

<style>
  .theme-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    color: var(--color-text-secondary);
    transition: color 200ms ease;
  }

  .theme-toggle:hover {
    color: var(--color-text-primary);
  }

  /* Both icons live in the DOM at all times; only the one matching the
     current theme is shown, driven purely by the html[data-theme]
     attribute — no per-instance JS state, so this works identically
     whether there's one toggle on the page or several (desktop header
     + mobile menu). */
  .icon-sun,
  .icon-moon {
    display: none;
  }

  :root[data-theme='dark'] .theme-toggle .icon-sun {
    display: block;
  }

  :root[data-theme='light'] .theme-toggle .icon-moon {
    display: block;
  }
</style>

<script>
  const toggles = document.querySelectorAll<HTMLButtonElement>('[data-theme-toggle]');
  toggles.forEach((toggle) => {
    toggle.addEventListener('click', () => {
      const current = document.documentElement.getAttribute('data-theme');
      const next = current === 'light' ? 'dark' : 'light';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
    });
  });
</script>
